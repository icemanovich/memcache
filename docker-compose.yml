version: '3.4'

services:

  app:
    build: .
    volumes:
      - ./:/var/www/service
    env_file:
      - .env
    networks:
      - mem

  memcached:
    image: 'bitnami/memcached:latest'
    networks:
      - mem


networks:
  mem: